var express = require("express");
var mongoose = require("mongoose");
var League = require("../models/team.model");
var utils = require("../utils")

var router = express.Router();

module.exports.routes = router;